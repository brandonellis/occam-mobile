# Shared helper: select a service by name.
# Mirrors the pattern of clicking a service card by text in the web tests.
# Requires env var: SERVICE_NAME
appId: com.occamgolf.app

---

- tapOn:
    text: ${SERVICE_NAME}
- waitForAnimationToEnd
